# KIRITO: BTC 5m + 15m opposite strategies. Copy .env.example -> .env
services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./exports:/app/exports
      - bot_data:/app/data

volumes:
  bot_data:
